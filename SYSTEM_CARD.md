# 시스템 카드 (System Card) — 주머니 (ZooMoney)

> 이 문서는 저장소의 실제 코드(2026-07-28 기준, `main` HEAD `5f84da3`)를 읽고 작성했습니다.
> README/FEATURES.md의 서술과 실제 코드가 어긋나는 부분이 있으면 코드를 기준으로 기술했습니다.

## 1. 이 앱은 무엇인가

주머니(ZooMoney)는 목표 상품을 정하고 그 목표를 향해 저축하면 캐릭터가 성장하는 게이미피케이션 저축 습관 앱입니다. 프론트엔드는 프레임워크 없는 Vanilla JS 정적 웹앱이며, 상태는 브라우저 `localStorage`에 저장됩니다(`js/state.js`, `STORAGE_KEY = 'piggyquest_state_v1'`).

핵심 루프: 온보딩(목표/예산 설정) → 매일 지출 기록 → 하루 정산(성공/실패 판정) → 캐릭터 성장·배지·저금통 잔액 반영 → 친구/랭킹으로 소셜 동기부여.

## 2. LLM 에이전트 — 의도된 용도

앱은 Dify(dify.ai) 기반 Chatflow 워크플로 **5개**를 사용합니다(GPT-4o-mini 기반, 일부는 Tavily 웹검색·Cohere RAG 지식베이스 연동). 각 에이전트의 의도된 역할:

| 에이전트 | 연결 파일 | 의도된 역할 |
|---|---|---|
| 목표설정 (`goal`) | `js/screens/onboarding.js` | 사용자가 말한 상품명으로 실시간 가격 검색 (Tavily) |
| 소비분석 (`analysis`) | `js/screens/onboarding.js` | 업로드된(실제로는 목업) 카드 내역의 카테고리 분류·이상치 탐지 대화 |
| 예산플래닝 (`budget`) | `js/screens/budget.js` | 카테고리별 월 지출·저축 목표를 바탕으로 절감 예산 제안 |
| 트레이너 챗봇 (`trainer`) | `js/screens/trainer_chat.js` | 절약 팁, 충동구매 상담, 목표 변경, 실시간 예산 계산 등 인텐트 분류형 대화 |
| 소비요약 (`summary`) | `js/modals.js`, `js/screens/mypage.js` | 하루 정산 코멘트, 월간 소비 리포트 코멘트 생성 |

**참고 — 문서 드리프트:** 2026-07-27 커밋(`5f84da3`, "Remove ETF coach feature")에서 ETF 추천 에이전트(`dify/etf-coach.yml`, `js/screens/etf.js`, 게이트웨이의 `AGENT_SECRETS.etf`)가 완전히 제거되었습니다. `README.md`와 `FEATURES.md`의 기능 목록(표)은 이 커밋에서 이미 업데이트되어 5개 에이전트만 나열하지만, 같은 문서의 서술 문장 일부(`README.md`의 "AI 워크플로 6종", `FEATURES.md`의 "Dify AI 에이전트 6개")는 여전히 옛 개수(6개)를 남기고 있어 표와 서술이 서로 어긋납니다. **현재 코드 기준 실제 활성 에이전트는 5개이며, ETF/투자 추천 기능은 존재하지 않습니다.** 이 시스템 카드는 이 실제 상태를 기준으로 작성했습니다.

## 3. 사용자 데이터 흐름 — 정확히 어디로 가는가

**"localStorage에 저장된다" ≠ "기기 밖으로 나가지 않는다"입니다.** 이 둘은 다른 이야기이고, 이 앱은 두 가지가 동시에 사실입니다:

1. **저장(at rest):** 목표 금액, 월 생활비, 카테고리별 지출, 저금통 잔액, 캐릭터/배지 상태 등 앱의 모든 상태는 서버 DB 없이 브라우저 `localStorage`에만 저장됩니다(`js/state.js`의 `persist()`/`loadFromStorage()`). 이 부분만 보면 "서버에 사용자 DB가 없다"는 말은 맞습니다.
2. **전송(in transit):** 그러나 AI 기능이 호출될 때마다, 위 재무 데이터의 상당 부분이 **평문 자연어 메시지(`query` 필드)에 실려 기기 밖으로 나갑니다.** 구체적으로 `js/screens/budget.js`(`_bdgFetchSuggestions`, 76행 부근)는 사용자의 **월 저축 목표 금액**과 **카테고리별 월 평균 지출액**을 사람이 읽는 문장으로 조립해 Dify에 보냅니다. `js/screens/onboarding.js`의 소비분석 흐름도 (목업이긴 하지만) 카드 지출 내역을 대화 메시지로 Dify에 전달합니다. 트레이너 챗봇은 사용자가 직접 입력한 고민·목표 관련 텍스트를 그대로 전송합니다.

전송 경로는 두 가지 모드로 동작합니다(`js/screens/onboarding.js`의 `callDifyAPI()`):

- **로컬 키 모드** (`js/config.js`가 있을 때): 브라우저 → `https://api.dify.ai/v1/chat-messages` (Dify API에 직접, `Authorization: Bearer <키>` 헤더 포함)
- **게이트웨이 모드** (라이브 데모, `js/config.public.js`): 브라우저 → Cloudflare Worker(`gateway/src/index.js`, `/chat`) → `https://api.dify.ai/v1/chat-messages` (키는 Worker secret에만 존재)

두 경로 모두 최종적으로 **Dify(dify.ai, 제3자 SaaS)** 로 사용자 재무 데이터가 도달하고, Dify 내부에서 다시 **OpenAI GPT-4o-mini**(모델 추론), 목표설정 에이전트의 경우 **Tavily**(실시간 웹검색), 소비분석/예산플래닝/트레이너 에이전트의 RAG 노드는 **Cohere 지식베이스**로 데이터·쿼리가 추가로 전달될 수 있습니다(`README.md` 기술 스택 표 기준).

**요약:** 이 앱이 "서버 DB 없음, localStorage만 사용"이라고 설명하는 것은 저장 방식에 대해서는 정확하지만, **AI 기능을 한 번이라도 쓰면 목표 금액·지출 카테고리·고민 내용이 Cloudflare, Dify, OpenAI, Tavily/Cohere 등 최소 3~4개 제3자 서비스로 전송된다는 사실을 가리는 표현**이 될 수 있습니다. "로컬 저장 = 데이터가 기기를 떠나지 않음"은 이 앱에는 성립하지 않습니다.

## 4. 알려진 한계

- **온보딩 카드 업로드는 시뮬레이션입니다.** 실제 카드사/은행 API 연동이 없고, 카드사 선택 UI와 무관하게 항상 `js/screens/onboarding.js`의 하드코딩된 `CARD_EXPENSES` 목업 배열을 사용합니다(`FEATURES.md`에도 이 사실이 명시돼 있음).
- **소비분석 에이전트의 대화형 결과는 실제 예산 계산에 반영되지 않습니다.** Dify 소비분석 챗봇이 만드는 구조화 JSON(이상치 판단 등)은 채팅 UX 표시용일 뿐이고, 실제 카테고리 집계는 별도의 로컬 규칙 기반 분류기가 수행합니다.
- **서버 측 사용자 계정/백업이 없습니다.** 모든 상태가 단일 기기의 `localStorage`에만 있어, 브라우저 데이터 삭제·기기 변경 시 진행 상황이 소실되고 다중 기기 동기화도 없습니다.
- **호출 한도:** 게이트웨이는 IP당 하루 40회로 AI 호출을 제한합니다(`gateway/src/index.js`, `DAILY_LIMIT = 40`, 2026-07-28 기준 코드로 확인). 이는 남용 방지용이며 실사용자 트래픽을 감당하도록 설계된 한도가 아닙니다(자세한 내용은 `docs/load-design.md`).
- **접근성 미검증.** 정적 코드 리딩 기준으로 다수의 접근성 격차가 발견되었습니다(`docs/accessibility-review.md` 참고) — 실제 스크린리더/키보드 사용자 테스트는 수행되지 않았습니다.
- **문서-코드 드리프트:** 위 2절에서 언급한 것처럼 README/FEATURES의 에이전트 개수 서술(6개)이 실제 코드(5개, ETF 코치 제거 후)와 어긋나는 부분이 남아있습니다.

## 5. 컴플라이언스/거버넌스 메모 — 투자 관련 안내에 대해

- **현재 코드에는 투자(ETF 등) 추천 기능이 없습니다.** 2026-07-27 커밋에서 ETF 코치 에이전트가 완전히 제거되었고, 남은 5개 에이전트는 저축·예산·지출 분석 범위입니다. 이는 컴플라이언스 리스크를 낮추는 방향의 변경입니다.
- **다만 "예산플래닝" 에이전트는 여전히 재무적 의사결정에 영향을 주는 제안(카테고리별 지출 절감액)을 생성합니다.** 이는 투자자문이 아니라 가계부/예산 관리 조언에 가깝지만, 실사용자에게 노출하기 전에는 "이 앱의 제안은 재무·투자 자문이 아니며 참고용 시뮬레이션"이라는 고지가 필요합니다. 현재 코드/UI에서 이런 고지 문구는 확인되지 않았습니다.
- **향후 ETF 코치류 기능을 재도입할 경우** (커밋 로그 상 과거에 존재했던 기능이므로 재도입 가능성을 배제할 수 없음) 다음이 선행되어야 합니다:
  - 명확한 투자자문 면책 고지 (이 앱은 자본시장법상 투자자문업으로 등록되지 않음을 명시)
  - 생성된 추천이 특정 종목·상품을 지목하지 않도록 하는 가드레일 검토
  - 실사용자 데이터(목표 금액, 저축 여력)가 제3자 LLM 서비스로 전송되는 것에 대한 별도 동의 절차
- **개인정보 관점:** 현재 앱은 이름(닉네임) 외의 실명·연락처·실제 계좌 정보를 수집하지 않는 것으로 코드상 확인됩니다(카드 데이터는 목업). 다만 3절에서 설명한 것처럼 목표 금액·지출 패턴 자체가 민감할 수 있는 재무 정보이므로, 실사용자 배포 전에는 최소한 "AI 기능 사용 시 입력 내용이 제3자 서비스로 전송됩니다"라는 고지가 필요합니다.

## 6. 의도된 사용 범위 (요약)

- **의도된 사용:** 캡스톤 프로젝트/포트폴리오용 저축 습관 게이미피케이션 데모. 실제 재무 관리 시스템으로서의 정확성·규제 준수는 보장되지 않습니다.
- **의도되지 않은 사용:** 실제 은행/카드 데이터 연동, 실사용자 대상 투자자문, 민감한 실명 재무 정보의 영구 저장.
